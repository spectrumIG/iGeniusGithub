query ListAllUserRepoQuery{
  viewer {
    repositories(first: 50) {
      totalCount
      nodes {
        name,
        languages(first: 1){
          nodes{
            name
          }
        }
        description
      }
      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
}