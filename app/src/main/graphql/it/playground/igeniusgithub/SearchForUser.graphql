query SearchForUser($filter: String!) {
  search(query: $filter, type: USER, first: 20) {
    nodes {
      ... on User {
        id
        email
        avatarUrl
        bio
        followers {
          totalCount
        }
        following {
          totalCount
        }
        login
      }
    }
  }
}